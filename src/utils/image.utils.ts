import { ExecutionData } from "../models/tiled-gameboy-tool-types";
export const getTilesetImageData =  (executionData:ExecutionData)=>{


    executionData.tilesets.forEach( (tileset)=>{

       
    })

}